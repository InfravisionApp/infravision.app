{
  "name": "infravision.app",
  "version": "1.0.0",
  "description": "Discord Community Engagement & Monitoring Platform",
  "author": "Scree",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "deploy": "rm -rf /var/www/infravision.app/* && cp -rf /home/scree/infravision/dist/* /var/www/infravision.app/",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "lintfix": "eslint --fix --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^1.0.0",
    "eslint-config-prettier": "^4.1.0",
    "eslint-plugin-prettier": "^3.0.1",
    "nuxt-purgecss": "^0.2.1",
    "prettier": "^1.16.4"
  }
}
